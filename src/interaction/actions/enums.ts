enum Giveaway{
    Join = 'join',
    Leave = 'leave'
}

enum Survey {
    Attendees = 'attendees'
}

enum General{
    Cancel = 'cancel'
}

enum Locale{
    Locale = 'locale'
}

export const Actions = {
    Giveaway,
    Survey,
    General,
    Locale
}